import styled from "@emotion/styled/macro"

const CPageContainer = styled.article(`
    width: calc(100% - 20px);
    max-width: 400px;
    padding: 20px;
    background-color: white;
    border-radius: 5px;
    height: 100%;
`)

export default CPageContainer