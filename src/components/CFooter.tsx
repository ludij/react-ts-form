import styled from "@emotion/styled/macro"

const CFooter = styled.article(`
    display: flex;
    justify-content: space-around;
    align-items: center;
    flex-wrap: wrap;
    font-size: 10px;
    color: rgb(200, 200, 200);
    margin-top: 40px;
`)

export default CFooter